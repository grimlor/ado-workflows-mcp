# ado-workflows-mcp

MCP server exposing [ado-workflows](https://github.com/grimlor/ado-workflows) as tool calls for AI agents.

## Development

```bash
direnv allow   # activates venv, syncs deps, configures git identity
task check     # lint + type + test
```
